<% include header.ejs %>
<script type="text/javascript">
$(document).ready(function(){
	$("#username").blur(function(){
		var username=$(this).val();
		if(username.length<=6){
		$("#nametip").html("<font color=red >&#10006用户名应为数字字母组合且不少于6个字符！</font>");
		return false;
		}else{
		var username=$(this).val();
        var url="/find?username="+username;
		//alert(url);
		$.get(url,null,function(data){
		if(data){
		$("#nametip1").html(username);
		$("#nametip2").html("<font color=red >&#10006这个已被注册了，换个吧</font>");
		}else{
		$("#nametip2").html("<font color=green >长度正确且可以注册！</font>");
		}
		});
		}
		});
	$("#password").blur(function(){
		var pwd=$(this).val();
		if(pwd.length<=5){
		$("#pwdtip").html("<font color=red >&#10006密码建议为数字大写字母和小写字母组合且不少于5个字符！</font>");
		return flase;
		}else{
		$("#pwdtip").html("<font color=green >&#10003</font>");
		return true;
		}
		});
	$("#password2").blur(function(){
	    var pwd=$("#password").val();
	    var pwd2=$(this).val();
	    if(pwd!=pwd2){
	    $("#pwd2tip").html("<font color=red >&#10006两次密码不一致，请重新输入！</font>");
		return flase;
		}else if(pwd2.length<=4){
		$("#pwd2tip").html("<font color=red >&#10006密码太短了！</font>");
		return false;
		}else{
		$("#pwd2tip").html("<font color=green >&#10003</font>");
		}
	});
});
</script>
<script>

// Example 1
$('#password').hideShowPassword({
  // Creates a wrapper and toggle element with minimal styles.
  innerToggle: true,
  // Makes the toggle functional in touch browsers without
  // the element losing focus.
  touchSupport: Modernizr.touch
});
<!--password 可见-->
</script>
<div class="container">
<div class="row" >
<div class="col-xs-6 col-md-offset-3">
<div class="panel panel-success">
<div class="panel-heading">
 <h3 class="panel-title">用户注册</h3>
</div>
 <div class="panel-body">
<form role="form" id="regForm" method="get" action="/reg">
<fieldset>
  <div class="form-group">
    <label for="username">用户名称</label>
    <input type="text" class="form-control" name="username" id="username" placeholder="请输入注册用户名" required>
     <label for="username" id="nametip">*</label> 
     <label for="username" id="nametip2"></label> 
  </div>
   <div class="form-group">
    <label for="password">密码</label>
    <input type="password" class="form-control" name="password" id="password" placeholder="请输入用户密码" required>
    <label for="password" id="pwdtip">*</label> 
  </div>
  <div class="form-group">
    <label for="password2">确认密码</label>
    <input type="password" class="form-control" name="password2" id="password2" placeholder="请输入用户密码" required>
    <label for="password2" id="pwd2tip">*</label>
  </div>
    <button id="sub" type="submit" class="submit btn btn-primary">注册用户</button>
    <button type="reset" class="btn btn-info">清除信息</button>
    <label>
            <input type="checkbox" value="remember-me"> 其它选项
            <a href="/login">已有账户，点此登录</a>
     </label>
 </fieldset>   
</form> 

</div>
</div>
<!--居中对其-->
</div>
<!--Row的边界-->
</div>
<!--容器的边界--> 
</div>
<body>
<% include footer.ejs %>
